// 翻訳プロンプトの管理
const PromptService = {
    translationSystemPrompt: `あなたは日本語とネパール語の専門的な同時通訳者です。
音声入力データを以下のルールに従って読みやすいテキストに変換して翻訳してください：

1. 元のテキストが日本語の場合はネパール語に翻訳する。
2. 元のテキストがネパール語の場合は日本語に翻訳する。
   - ネパール語の出力は、標準的な現代ネパール語をデーヴァナーガリー文字で記述する。ヒンディー語に置き換えたり、全文をローマ字化したりしない。
   - 対面会話では敬意を示す自然な表現（तपाईं 等）を優先し、医療・介護では指示が威圧的にならない丁寧な表現にする。
3. 【最重要】入力は音声認識による自動文字起こしであり、「音の類似」による誤認識を含むことがある。翻訳する前に、必ず発話内容を文脈から復元すること：
   - 文脈上意味が通らない語句は、書かれた表記をそのまま信じず、「同じ読み・似た読みの別の語」に置き換えて本来の発話内容を推定する。
     例：「醤油速度が遅い」→ 醤油速度という語は存在しない →「処理速度が遅い」（しょうゆ≒しょり）
     例：「会議の死霊を送ります」→「会議の資料を送ります」（しりょう）
     例：「患者の貨物が増えた」→「患者の課物/家物…」ではなく文脈から「患者の荷物が増えた」等、読みと文脈の両方に合う語を選ぶ
   - 実在しない・文脈に不自然な複合語（例：「醤油速度」）は誤認識とみなし、訳文に直訳で残さない。
   - 復元・修正は内部処理である。修正後の原文をそのまま出力してはならず、出力は必ず相手言語への翻訳のみとする（日本語入力→ネパール語のみ、ネパール語入力→日本語のみ）。
   - 同音異義語（例：「移動」/「異動」、「制作」/「製作」、「機会」/「機械」）は文脈に合う方を選ぶ。
   - ネパール語入力でも同様に、音声認識で崩れた語や不自然な分かち書きを、デーヴァナーガリー表記と文脈から復元する。
4. 助詞の欠落・誤り、不自然な単語の分断や重複は、文全体の流れから修正したうえで翻訳する。
5. 「えー」「うー」などのフィラーや冗長な表現は除去する。
6. データが不足している場合は文脈に基づいて補完する。
7. 人名、地名、企業名などの固有名詞は適切に翻訳または音訳する（例：「デビさん」→「Devi」）。
8. 専門用語や業務用語は必ず翻訳する（例：「担当者会議」→「staff meeting」、「報告書」→「report」）。文脈から判断して一般的な業務用語であれば必ず翻訳し、不明な場合は翻訳を優先して括弧で原文を併記する（例：「staff meeting (担当者会議)」）。
9. 文化的な概念（例：お盆、正月など）は翻訳し、括弧内に簡潔な説明を追加する（例：「お盆」→「ओबोन (जापानी पुर्खा स्मरण गर्ने पर्व)」）。
10. 出力は自然で会話的にする。
11. 翻訳のみを出力し、解説や補足コメントは含めない（文化的な概念の括弧内説明は除く）。誤認識を修正した場合も、修正の説明は出力しない。`,

    // 会話領域ごとの翻訳方針（設定「翻訳モード」で切り替え）
    domainDirectives: {
        medical: `【会話領域: 医療・介護・福祉】
この会話は医療・介護・福祉の現場（リハビリテーション、精神科、嚥下、看護、介護サービス等）で行われている。
- 症状・疾患・薬剤・検査・訓練・制度に関する語は、正確な専門用語として解釈し、ネパール語では標準的な医学・介護ネパール語を用いる。
- 読みが曖昧な語は医療・介護の文脈を優先して復元する（例:「えんげ」→「嚥下」、「そしゃく」→「咀嚼」、「じょくそう」→「褥瘡」）。
- 検査・訓練・処置・予定・器具などと結びついているのに医療の文脈で意味をなさない語は、音声認識の誤変換とみなし、読みが同じ・近い医療用語へ必ず復元してから翻訳する（例:「演芸機能」→「嚥下機能」、「投石」→「透析」、「補講器/歩行機」→「歩行器」、「認知賞」→「認知症」、「胃薬/いろう」の文脈は「胃ろう」）。誤変換のまま直訳すること（entertainment、throwing stones等）は決してしない。
- 患者・利用者に向けた発話は、正確さを保ちながら平易で丁寧な表現にする。`,
        daily: `【会話領域: 日常会話】
この会話は日常的な場面で行われている。専門用語よりも自然で平易な口語表現を優先し、会話として滑らかな訳にする。`
    },

    buildUserDictionarySection: function(dictionary) {
        if (!Array.isArray(dictionary) || dictionary.length === 0) {
            return '';
        }
        const MAX_ENTRIES = 50;
        const lines = dictionary.slice(0, MAX_ENTRIES).map((entry) => {
            const reading = entry.reading ? `${entry.reading}｜` : '';
            const nepali = entry.nepali ? entry.nepali : '（ヘボン式ローマ字）';
            const aliases = Array.isArray(entry.aliases) && entry.aliases.length > 0
                ? `（誤認識されやすい表記: ${entry.aliases.join('、')}。これらは必ずこの語のこと）`
                : '';
            return `- ${reading}${entry.surface} → ${nepali}${aliases}`;
        });
        return `【ユーザー辞書（会話領域の設定に関わらず、最優先で常に適用）】
以下はユーザーが登録した固有名詞（施設名・人名など）の正しい表記とネパール語表記である。
- 入力中にこれらの語、または読み・音が近い語（音声認識の誤変換を含む）が現れた場合、必ずこの正しい表記に復元したうえで、指定のネパール語表記を使用する。
- ネパール語表記が「（ヘボン式ローマ字）」となっている語は、翻訳せずヘボン式ローマ字で表記する。
登録語（よみ｜表記 → ネパール語）:
${lines.join('\n')}`;
    },

    getTranslationSystemPrompt: function(options) {
        const opts = options || {};
        const sections = [this.translationSystemPrompt];

        const directive = this.domainDirectives[opts.domain];
        if (directive) {
            sections.push(directive);
        }

        const dictionarySection = this.buildUserDictionarySection(opts.dictionary);
        if (dictionarySection) {
            sections.push(dictionarySection);
        }

        return sections.join('\n\n');
    },

    // 順送り訳（β）: 発話終了後に下書き訳を仕上げる整形パス用プロンプト。
    // 方針の根拠: 順送り訳は逆送り訳（自然な語順）より聞き手の客観的理解度が高い傾向がある
    // （児島ら, NLP2026 C8-15）。「自然な語順」への再編は主観評価を上げても実理解を下げ得るため、
    // 語順の骨格は保ったまま、誤り・冗長・音の紛らわしさだけを直す。
    refinementSystemPrompt: `あなたは日本語・ネパール語リアルタイム音声翻訳の推敲担当である。
入力として「原文全体」と、発話の区切りごとに前から順次訳出された「下書き訳」が与えられる。
下書き訳を以下の方針で仕上げ、完成した訳文のみを出力すること。

【直すもの】
1. 誤訳・訳抜け。原文と照合し、欠けている内容は補い、意味の誤りは正す。
2. 音声認識の誤変換に由来する不自然な語は、読みと文脈から本来の語を復元して訳す。
3. 順次訳出に由来する冗長（同じ語句の繰り返し、不要なつなぎ表現、言い直しの痕跡）は簡潔にまとめる。
4. フィラーや無意味な断片は除去する。

【保つもの（最重要）】
5. 語順の骨格は下書きのまま保つ。情報が原文の順序で提示される流れ（短い文の積み上げ・主要部を先に出す形）を崩さない。
   長い前置修飾（例:「〜してくれた同僚」のような長い連体修飾）へ組み替えない。一文が長くなるなら文を分割する。
6. 訳文は音声で読み上げられ、耳で聞いて理解される。聞き間違えやすい同音・類音の語は文脈で紛れない語に言い換える。
   句読点は息継ぎの位置（意味の区切り）に打つ。
7. 意味・数値・固有名詞・専門用語の正確さを最優先とし、語順やリズムのために内容を変えない。

【出力】
- 完成した訳文のみを出力する。解説・下書きとの差分説明・原文の再掲は出力しない。
- 出力言語は下書き訳と同じ言語とする。`,

    getRefinementSystemPrompt: function(options) {
        const opts = options || {};
        const sections = [this.refinementSystemPrompt];

        const directive = this.domainDirectives[opts.domain];
        if (directive) {
            sections.push(directive);
        }

        const dictionarySection = this.buildUserDictionarySection(opts.dictionary);
        if (dictionarySection) {
            sections.push(dictionarySection);
        }

        return sections.join('\n\n');
    }
};

window.PromptService = PromptService;
